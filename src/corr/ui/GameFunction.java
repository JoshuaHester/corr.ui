package corr.ui;

/*
This package makes use of the command pattern to simplify things like button presses.
Use a lamda function declared as a GameFunction object.
*/

public interface GameFunction {
    public void execute();
}
